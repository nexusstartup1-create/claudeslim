# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Claude Code settings are generated per machine by `cslim install` and contain
# an absolute path to the installed binary. They are not portable and must not
# be committed.
.claude/settings.json
.claude/settings.local.json
.claude/*.bak

# Benchmark scratch space
bench/.probe/
bench/.inject/

# OS
.DS_Store
Thumbs.db

# source archive for the 3D model (the processed .glb is committed instead)
*.rar

# the marketing site lives here but is deployed straight to Vercel,
# not through this repository
docs/

# empty Next.js scaffold, kept locally
cslim-site/
